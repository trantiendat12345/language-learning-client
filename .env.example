# Để trống để dùng đường dẫn tương đối (/api/...) qua Vite proxy (server.proxy trong
# vite.config.ts) - cần khi expose frontend qua tunnel (Cloudflare Tunnel, ngrok...) vì browser
# chỉ thấy 1 origin duy nhất, tránh vỡ CORS/cookie SameSite. Set URL đầy đủ nếu gọi thẳng backend.
VITE_API_BASE_URL=
